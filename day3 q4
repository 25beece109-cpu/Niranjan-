#include <stdio.h>

int main() {
    int n, lastDigit, exceptLast;
    printf("Enter a number: ");
    scanf("%d", &n);
    lastDigit = n % 10;     
    exceptLast = n / 10;     
    printf("%d", lastDigit);
    printf("%d", exceptLast);
    return 0;
}